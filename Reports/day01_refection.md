# Day 1 Reflection

The concept that clicked most quickly for me was working with Python
collections, especially lists and dictionaries. I understood how lists can
store ordered values while dictionaries make it easier to associate values
with meaningful keys.

The concept I still find slightly confusing is NumPy broadcasting. I understand
that NumPy can perform operations between arrays with different shapes, but I
still want to better understand how NumPy decides whether two shapes are
compatible. The example of subtracting column means makes sense, but I would
like more practice with less obvious shapes.

One question I would like answered at the beginning of tomorrow's session is:
how can I quickly determine whether two NumPy arrays can be broadcast together
before performing an operation?

I spent approximately 3 hours completing the assignment, including setting
up the environment, writing the notebook, testing the functions, and reviewing
the results.